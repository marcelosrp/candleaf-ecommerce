import styled from 'styled-components'

export const Header = styled.header`
  align-items: center;
  display: flex;
  height: 7.5rem;
  justify-content: space-between;
  margin: 0 auto;
  width: var(--container);
`
