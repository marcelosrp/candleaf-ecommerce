import styled from 'styled-components'

export const WrapperProduto = styled.section`
  margin: 0 auto;
  max-width: 100%;
  padding: 5rem 0;
  width: var(--container);
`

export const WrapperItem = styled.div``
